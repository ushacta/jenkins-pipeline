pipeline {
   agent any

   stages {
      stage('install') {
         steps {
            echo 'downloading'
         }
      }
       
       stage('deploy') {
         steps {
            echo 'Will deploy the site now'
         }
      }
   }
}
